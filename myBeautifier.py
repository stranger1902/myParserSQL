
class MyBeautifier():

    def __init__(self): pass

    '''
    def visitNode(self, ast_node):

        if ast_node['type'] == '_____': return _____(ast_node)
        
        elif ast_node['type'] == '_____': return _____(ast_node)
        
        else: raise Exception(f"Type node '{ast_node['type']}' in NOT valid")

    def visitAST(self, AST): 

        for item in AST.items():

            self.visitNode(item)

            self.visitAST(item)
    '''